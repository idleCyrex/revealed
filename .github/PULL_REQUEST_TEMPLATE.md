## What does this change?

<!-- A short description of the change and why it's needed. -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Docs (`apps/docs` and/or `packages/revealed/README.md`)
- [ ] Chore / tooling

## Checklist

- [ ] `npm run build:lib` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] No runtime dependency was added to `packages/revealed` (peer deps on
      `react`/`react-dom` for the `revealed/react` entry point are fine)
- [ ] `CHANGELOG.md` updated under `## Unreleased` for any user-visible change
- [ ] Docs updated if public API/behaviour changed (`apps/docs`, and
      `packages/revealed/README.md` if it's a library-level change)

## Related issue

<!-- Closes #... -->
